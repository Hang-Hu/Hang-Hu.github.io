---
layout: post
author: Hang Hu
categories: mysql
tags: Mysql 
cover: 
---

```
//修改分隔符为$:
DELIMITER $
CREATE TRIGGER WRITELOG
AFTER INSERT ON COMPANY 
FOR EACH ROW//行级触发器
BEGIN 
INSERT INTO AUDIT VALUES(NEW.ID,NOW()); //NEW表示插入这行,NOW表示现在的时间
END$
DELIMITER ;//改回换行符
```
```
BEGIN;
DELETE FROM COMPANY WHERE AGE=25;//删除的使用，类似于select
ROLLBACK;//回滚,也可以用COMMIT提交
```
