# lineage doesn't ask me for authentication adb


## Solution

1. Open root access for apps and adb in developer options.

2. open `USB for file transfer`, select transfer files.

Then the authorization window will be prompted.